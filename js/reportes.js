import { setupApp, pageShell, card, metrics, money, transactionsTable, chartData, dateKey, salesForDate, recordDate } from "./app.js";
let selectedDate = new Date().toISOString().slice(0,10);
function chartBars(items, valueKey, formatter) { const values = Object.values(items).slice(-12); const max = Math.max(...values.map(item => item[valueKey]), 1); const totals = values.reduce((sum, item) => sum + Number(item[valueKey] || 0), 0); const maxCash = values.reduce((sum, item) => sum + Number(item.sales || 0), 0); const maxCredit = values.reduce((sum, item) => sum + Number(item.credit || 0), 0); const maxPayments = values.reduce((sum, item) => sum + Number(item.payments || 0), 0); const cashEnd = totals ? maxCash / totals * 100 : 0; const creditEnd = totals ? cashEnd + maxCredit / totals * 100 : 0; return values.length ? `<div class="chart-visual"><div class="chart-list">${values.map(item => `<div class="bar-row"><span>${item.label}</span><span class="bar-track"><span class="bar-fill" style="width:${Math.max(3, item[valueKey] / max * 100)}%"></span></span><strong>${formatter(item[valueKey])}</strong></div>`).join("")}</div><div class="donut-wrap"><div class="donut" style="background:conic-gradient(var(--chart-cash) 0 ${cashEnd}%, var(--chart-credit) ${cashEnd}% ${creditEnd}%, var(--chart-payments) ${creditEnd}% 100%)"><span>${formatter(totals)}</span></div><div class="donut-legend"><span><i class="legend-dot cash"></i>Contado</span><span><i class="legend-dot credit"></i>Crédito</span><span><i class="legend-dot payments"></i>Pagos</span></div></div></div>` : `<div class="empty-state">Aún no hay registros suficientes para graficar.</div>`; }
function render(state) { const m = metrics(state); const selected = salesForDate(state.ventas, selectedDate); const cash = selected.filter(sale => sale.tipo === "Contado").reduce((sum, sale) => sum + Number(sale.total), 0); const credit = selected.filter(sale => sale.tipo === "Credito").reduce((sum, sale) => sum + Number(sale.total), 0); const paid = selected.filter(sale => ["Abono","Cancelado"].includes(sale.tipo)).reduce((sum, sale) => sum + Number(sale.total), 0); const data = chartData(state, selectedDate); const monthly = Object.fromEntries(Object.entries(data.monthly).map(([key, value]) => [key, { ...value, label:key }])); const daily = Object.fromEntries(Object.entries(data.daily).map(([key, value]) => [key, { ...value, label:key.slice(5) }])); pageShell("Reportes diarios", "Revisa cada registro por fecha y separa contado, crédito y pagos.", "reportes", `<section class="panel"><div class="form-actions"><label style="max-width:220px">Día del reporte<input id="reportDate" type="date" value="${selectedDate}"></label><span class="notice">El historial conserva todas las operaciones en Firestore.</span></div></section><section class="section-title"><div><h2>Resumen del día</h2><p>${selectedDate === dateKey() ? "Fecha actual" : "Fecha seleccionada"}</p></div></section><section class="cards">${card("Contado", money(cash), "Ventas pagadas en el día", "orange")}${card("Crédito", money(credit), "Ventas fiadas en el día", "red")}${card("Pagos recibidos", money(paid), "Abonos y cancelaciones", "blue")}${card("Movimientos", selected.length, "Registros del día", "")}</section><section class="section-title"><div><h2>Gráficos de actividad</h2><p>Comparación mensual y detalle diario de ventas, créditos y pagos.</p></div></section><section class="grid-two"><div class="panel"><h2>Últimos meses</h2>${chartBars(monthly, "sales", money)}</div><div class="panel"><h2>Días registrados</h2>${chartBars(daily, "sales", money)}</div></section><section class="grid-three"><div class="panel"><h2>Crédito mensual</h2>${chartBars(monthly, "credit", money)}</div><div class="panel"><h2>Pagos mensuales</h2>${chartBars(monthly, "payments", money)}</div><div class="panel"><h2>Inventario del día</h2><p class="metric-value">${data.inventory}</p><p class="metric-note">Movimientos de stock registrados</p></div></section><section class="section-title"><div><h2>Registros del día</h2><p>Incluye ventas, abonos, cancelaciones y saldos manuales.</p></div></section><section class="panel">${transactionsTable(selected)}</section><section class="section-title"><div><h2>Acumulado general</h2><p>Comparativa desde el primer registro.</p></div></section><section class="grid-three"><div class="panel"><div class="summary-row"><span>Ingresos en caja</span><strong>${money(m.cashTotal)}</strong></div><div class="summary-row"><span>Unidades vendidas</span><strong>${m.totalUnits}</strong></div></div><div class="panel"><div class="summary-row"><span>Contado acumulado</span><strong>${money(state.ventas.filter(sale => sale.tipo === "Contado").reduce((sum, sale) => sum + Number(sale.total), 0))}</strong></div><div class="summary-row"><span>Crédito pendiente</span><strong>${money(m.debt)}</strong></div></div><div class="panel"><div class="summary-row"><span>Productos</span><strong>${state.inventario.length}</strong></div><div class="summary-row"><span>Clientes con deuda</span><strong>${Object.keys(state.creditos).length}</strong></div></div></section>`); document.querySelector("#reportDate").addEventListener("change", event => { selectedDate = event.target.value; render(state); }); }
pageShell("Cargando...", "Preparando reportes.", "reportes", ""); setupApp(render);