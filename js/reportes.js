import { setupApp, pageShell, card, metrics, money, transactionsTable } from "./app.js";
let selectedDate = new Date().toISOString().slice(0,10);
function render(state) { const m = metrics(state); const selected = state.ventas.filter(sale => { const parts = sale.fecha.split(",")[0].split("/"); return parts.length === 3 && `${parts[2]}-${parts[1].padStart(2,"0")}-${parts[0].padStart(2,"0")}` === selectedDate; }); const cash = selected.filter(sale => sale.tipo === "Contado").reduce((sum, sale) => sum + Number(sale.total), 0); const credit = selected.filter(sale => sale.tipo === "Credito").reduce((sum, sale) => sum + Number(sale.total), 0); const paid = selected.filter(sale => ["Abono","Cancelado"].includes(sale.tipo)).reduce((sum, sale) => sum + Number(sale.total), 0); pageShell("Reportes diarios", "Revisa cada registro por fecha y separa contado, crédito y pagos.", "reportes", `<section class="panel"><div class="form-actions"><label style="max-width:220px">Día del reporte<input id="reportDate" type="date" value="${selectedDate}"></label><span class="notice">El historial conserva todas las operaciones en Firestore.</span></div></section><section class="section-title"><div><h2>Resumen del día</h2><p>${selectedDate === new Date().toISOString().slice(0,10) ? "Fecha actual" : "Fecha seleccionada"}</p></div></section><section class="cards">${card("Contado", money(cash), "Ventas pagadas en el día", "orange")}${card("Crédito", money(credit), "Ventas fiadas en el día", "red")}${card("Pagos recibidos", money(paid), "Abonos y cancelaciones", "blue")}${card("Movimientos", selected.length, "Registros del día", "")}</section><section class="section-title"><div><h2>Registros del día</h2><p>Incluye ventas, abonos, cancelaciones y saldos manuales.</p></div></section><section class="panel">${transactionsTable(selected)}</section><section class="section-title"><div><h2>Acumulado general</h2><p>Comparativa desde el primer registro.</p></div></section><section class="grid-three"><div class="panel"><div class="summary-row"><span>Ingresos en caja</span><strong>${money(m.cashTotal)}</strong></div><div class="summary-row"><span>Unidades vendidas</span><strong>${m.totalUnits}</strong></div></div><div class="panel"><div class="summary-row"><span>Contado acumulado</span><strong>${money(state.ventas.filter(sale => sale.tipo === "Contado").reduce((sum, sale) => sum + Number(sale.total), 0))}</strong></div><div class="summary-row"><span>Crédito pendiente</span><strong>${money(m.debt)}</strong></div></div><div class="panel"><div class="summary-row"><span>Productos</span><strong>${state.inventario.length}</strong></div><div class="summary-row"><span>Clientes con deuda</span><strong>${Object.keys(state.creditos).length}</strong></div></div></section>`); document.querySelector("#reportDate").addEventListener("change", event => { selectedDate = event.target.value; render(state); }); }
pageShell("Cargando...", "Preparando reportes.", "reportes", ""); setupApp(render);